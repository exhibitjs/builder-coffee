{
  "name": "exhibit-coffee",
  "version": "0.0.4",
  "author": "Callum Locke",
  "dependencies": {
    "coffee-script": "^1.9.2",
    "convert-source-map": "^1.1.0"
  },
  "devDependencies": {
    "babel": "^5.2.11",
    "babel-eslint": "^3.0.1",
    "eslint": "^0.20.0",
    "nodemon": "^1.3.7"
  },
  "files": [
    "lib",
    "src"
  ],
  "keywords": [
    "exhibit-plugin"
  ],
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src --out-dir lib",
    "prepublish": "eslint src && npm run build",
    "watch": "nodemon --exec 'npm run build' --watch src --watch index.js --watch package.json"
  }
}
